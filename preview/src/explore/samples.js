// Fictional meetings using BACKEND_CONTRACT §4.1. Replaced by the backend loader at integration.
import meetings from "./fixture-meetings.json";
export const lectures = meetings.slice(0, 8);
export const laterLectures = meetings.slice(8);
